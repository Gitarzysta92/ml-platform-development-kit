apiVersion: v1
kind: Service
metadata:
  name: runpod-connector
  labels:
    app: runpod-connector
    app.kubernetes.io/part-of: ml-platform
spec:
  selector:
    app: runpod-connector
  ports:
    - name: http
      port: 8080
      targetPort: 8080

